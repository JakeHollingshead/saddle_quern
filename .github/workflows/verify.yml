name: Verify

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check-repo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check links and installer/skill consistency
        run: bash scripts/check_repo.sh
